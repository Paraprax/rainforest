<h1>Products (<%= @products.length %>)</h1>
<%= link_to "New Product", "/products/new" %>

<% @products.each do |product| %>
  <p><h4><%= product.name %><p>
  <div> $<%= product.formatted_price %> </h4>
  <h5>"<%= product.description %>"</h5></div>
<% end %>
